" Enable extra features (e.g. when run systemwide). Must be before pathogen
set nocompatible

" Enable syntax highlighting
syntax on

set number
set history=1000
set ruler
set expandtab
set ts=4
set shiftwidth=4

" see http://qmx.me/2010/07/11/remove-trailing-whitespace-in-vim
match Todo /\s\+$/

